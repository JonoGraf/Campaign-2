views:
  - type: cards
    name: Player Cards
    filters:
      and:
        - type == "Player"
        - status == "Legend"
    order:
      - file.name
      - status
      - description
      - level
    image: note.image
    imageAspectRatio: 1
    imageFit: contain
